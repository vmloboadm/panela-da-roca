import { Card } from '@/components/ui/Card'
import { SectionTitle } from '@/components/ui/SectionTitle'
export default function PrecosPage() {
  return <div className="fadein"><Card><SectionTitle icon="🔎">Varredura de Preços</SectionTitle><p className="text-text-muted text-sm">Em construção — Fase 3.</p></Card></div>
}
