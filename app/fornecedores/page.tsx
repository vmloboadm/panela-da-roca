import { Card } from '@/components/ui/Card'
import { SectionTitle } from '@/components/ui/SectionTitle'
export default function FornecedoresPage() {
  return <div className="fadein"><Card><SectionTitle icon="🏪">Fornecedores</SectionTitle><p className="text-text-muted text-sm">Em construção — Fase 2.</p></Card></div>
}
